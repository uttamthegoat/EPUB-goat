{
  "name": "epub-goat",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && npm run copy-deps && npm run package",
    "copy-deps": "copyfiles -f node_modules/jszip/dist/jszip.min.js .",
    "clean": "rimraf jszip.min.js epub-goat.zip",
    "package": "bestzip epub-goat.zip manifest.json background.js reader.html reader.js jszip.min.js icons/ css/ fonts/"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "bestzip": "^2.2.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.5"
  }
}
